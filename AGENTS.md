# Stock Agent 작업 지침

## 설계 문서 필수 규칙

다음 규칙은 이 저장소에서 유스케이스와 OOAD 설계를 작성하거나 검토할 때 반드시 적용한다.

1. 유스케이스는 사용자가 달성하려는 목표로 이름 짓는다. 검색, 클릭, 선택, 저장처럼 UI 또는 처리 단계를 각각 독립 유스케이스로 만들지 않는다.
2. 유스케이스 분석은 Essential Style로 작성한다. 특정 화면, 입력 기호, API, 데이터베이스, 내부 ID 또는 프레임워크를 교체해도 사용자 목표와 기본 흐름이 유지되어야 한다.
3. 유스케이스는 시스템을 Black box로 다룬다. 내부 저장소, 내부 서비스, 도메인 객체, DB 컬럼과 내부 식별자를 액터나 외부 관찰 결과로 표현하지 않는다.
4. 유스케이스 다이어그램은 액터와 사용자 목표 사이의 관계만 간결하게 보여준다. 상세 흐름, UI 동작, 데이터 저장 순서와 내부 컴포넌트 호출은 다이어그램에 넣지 않는다.
5. `<<include>>`는 여러 유스케이스가 반드시 수행하는 공통 사용자 목표를 재사용할 때만 사용한다. 실행 순서나 내부 처리 단계를 나타내는 화살표로 사용하지 않는다.
6. 유스케이스 다이어그램과 유스케이스 명세를 분리한다. 명세에는 최소한 ID, 이름, 목적, 주 액터, 트리거, 사전조건, 기본 흐름, 대안·예외 흐름과 성공 보장을 기록한다.
7. 사용자 목표와 기능 요구사항을 분리한다. `@` 입력, 자동완성, 내부 ID 연결, API 형식 같은 구체적 요구는 유스케이스 목적이 아니라 `FR-*` 요구사항 또는 UI·인터페이스 설계에 기록한다.
8. OOA를 먼저 완성한다. 사용자 목표, 외부에서 관찰 가능한 상호작용, 예외와 성공 보장이 합의되기 전에는 클래스, DB 테이블, API, 시퀀스 같은 OOD를 확정하지 않는다.
9. OOD를 작성할 때는 OOA의 유스케이스 또는 `FR-*` 요구사항과 추적 관계를 표시한다. 근거가 없는 객체, 저장 구조와 인터페이스를 미리 추가하지 않는다.
10. 시스템 경계를 명시한다. 사람과 실제 외부 시스템만 액터로 두고, 시스템 내부 DB나 내부 처리기는 외부 액터처럼 표현하지 않는다.
11. 설계 완료 전 다음 질문으로 자체 검토한다.
    - UI와 내부 구현을 완전히 교체해도 유스케이스 문장이 성립하는가?
    - 기본 흐름은 외부에서 관찰 가능한 사용자와 시스템의 상호작용인가?
    - 다이어그램에 내부 저장소, 내부 ID 또는 처리 순서가 노출되지 않았는가?
    - 모든 `include`가 공통 목표 재사용을 뜻하는가?
    - 각 OOD 요소가 어떤 유스케이스나 요구사항을 구현하는지 설명할 수 있는가?
