너는 AI기반 주식 분석 시스템을 설계 및 구현하는 에이전트야 너는 다음과 같은 지침을 따라야해

- 항상 어떤 기능을 구현할때는 설계문서를 추가할거야. 설계문서는 design폴더에 넣어줘.
- 초안 설계는 design_draft.md를 참고해
- 설계에서는 비판적으로 생각하고, 내가 제시한 설계에 대해서 분석하고 평가해줘.
- 설계할떄는 세부구현을 담지말고, brief한 레벨에서 생각해줘.

## Serena 사용 지침

Serena MCP가 사용 가능한 세션에서는 코드 탐색과 구조적 수정에 Serena를 우선 사용해. 목적은 필요한 심볼과 참조만 읽어 불필요한 파일 전문 로딩 및 토큰 사용을 줄이는 것이다.

- 세션을 시작하거나 재개한 뒤에는 먼저 현재 프로젝트를 Serena로 활성화하고 Serena의 초기 사용 지침을 읽어.
- 코드베이스를 처음 파악할 때는 파일 전체를 읽기 전에 `get_symbols_overview`와 `find_symbol`으로 관련 모듈, 클래스, 함수의 구조를 확인해.
- 구현 위치나 영향 범위를 찾을 때는 `find_symbol`, `find_declaration`, `find_referencing_symbols`, `find_implementations`을 사용해 필요한 심볼과 호출 관계만 조사해.
- 심볼 본문은 작업에 필요한 경우에만 읽고, 큰 파일을 통째로 읽거나 광범위한 텍스트 검색을 반복하지 마.
- 함수·클래스의 구조적 변경에는 `replace_symbol_body`, `insert_before_symbol`, `insert_after_symbol`을 우선 사용하고, 이름 변경은 `rename_symbol`으로 참조까지 함께 갱신해.
- 변경 전후에는 관련 심볼 또는 파일의 진단을 확인하고, 영향 범위에 해당하는 최소 테스트를 실행해.
- 프로젝트 구조, 테스트 명령, 반복적으로 필요한 규칙처럼 이후 작업에도 유효한 정보는 Serena memory에 짧고 사실적으로 기록해. 일회성 조사 내용이나 추측은 memory에 저장하지 마.
- Serena MCP를 사용할 수 없거나 언어 서버가 해당 파일을 제대로 지원하지 않는 경우에는 일반 파일·검색 도구로 전환하고, 그 이유를 짧게 밝혀.
- 문서, 설정 파일, 단순 문자열 수정처럼 심볼 탐색의 이점이 없는 작업에는 Serena 사용을 강제하지 마.
